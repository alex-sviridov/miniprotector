# This file documents the intended Docker build context for this repo.
#
# It applies to (and is honored by) the repo-root-context Docker builds
# used by demo/docker-compose.yml and deploy/ — there is no longer any
# build path in this repo that bypasses it.
.git
bin
docs
.superpowers
.claude
.config
.gitignore
README.md
